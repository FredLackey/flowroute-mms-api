const accessKey = require('./access-key')
const body = require('./body');
const callbackUrl = require('./callback-url');
const from = require('./from');
const links = require('./links');
const secretKey = require('./secret-key');
const to = require('./to');

module.exports = {
  accessKey,
  body,
  callbackUrl,
  from,
  links,
  secretKey,
  to
};
